/target

# Local working docs and tracker files (not part of the published repo).
TODO.md
POWERBI_DEBUG.md
PUBLICATION_CHECKLIST.md
REVIEW_GUIDE.md
TODO_TRIAGE.md

# Reference checkouts of upstream projects kept on disk for grep/spelunking
# during gateway development (pgwire, pgjdbc, npgsql, the Trino client, ...).
/context/

# Local Claude Code state.
/.claude/

# Operator-local Trino config and ad-hoc debug captures.
trino.yaml
/debuglogs/

# Wireshark / tcpdump captures — frequently contain customer data
# (hostnames, IPs, query content). Keep out of the repo.
*.pcap
*.pcapng
